Guide Me
Step: ADVANCE to Requirements & Scope
Goal: Establish boundaries for smart-meter ingestion, customer usage queries, tariff-based estimated cost, support inspection, and historical reprocessing.
Key Decisions: Raw readings are immutable, corrections must be supported, dashboards need low latency, and reprocessing must reuse historical raw data.

Guide Me
Step: ADVANCE to Capacity & Constraints
Goal: Size the system around ~580 readings/sec average, ~11.6k/sec burst, 5k dashboard RPS, 5-minute freshness, and p95 300 ms reads.
Key Decisions: Use a durable buffer for bursts, pre-aggregate dashboard reads, and avoid raw scans on the hot path.

Guide Me
Step: ADVANCE to Core Entities & Data Model
Goal: Model Customer, Meter, RawReading, Tariff, DailyAggregate, MonthlyAggregate, and calc_version.
Key Decisions: RawReading is append-only; duplicates are no-ops; corrections create a newer version; aggregates are derived and rebuildable.

Guide Me
Step: ADVANCE to APIs & Access Patterns
Goal: Define batch ingestion, usage reads, and admin reprocess access patterns.
Key Decisions: Provider ingest acks after durable enqueue; customer usage reads from aggregates; admin reprocess scans raw by range.

Guide Me
Step: ADVANCE to High-Level Architecture
Goal: Start the architecture with the write path only.
Do This Live:
1. Say: "I'll draw the provider write path first."
Write: `External Provider -> Ingest API -> Kafka -> Ingest Worker -> Postgres raw_readings`
Why: This satisfies burst buffering and no accepted reading should be lost.
